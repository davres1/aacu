Flask>=3.0
requests>=2.31
influxdb>=5.3
PyYAML>=6.0

# LLM gateway — speaks Ollama, OpenAI, Anthropic, Azure, Bedrock, Gemini, etc.
# with one API. Model selection is driven by env / setup.yaml.
litellm>=1.50

# Production WSGI server — gthread workers keep blocking Ansible calls
# off the request thread.
gunicorn>=22.0

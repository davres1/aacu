Flask>=3.0
requests>=2.31
influxdb>=5.3
PyYAML>=6.0
tenacity>=9.0
botocore>=1.30
# PDF report generation - pure-Python, no system libs required
reportlab>=4.0
Jinja2>=3.1
# LLM gateway — speaks Ollama, OpenAI, Anthropic (Claude), Google Gemini,
# Oracle OCI GenAI, Azure, Bedrock, etc. with one API. Model selection is driven
# by env / setup.yaml (chatbot.llm). Gemini works through LiteLLM's REST client
# with no extra dependency; Oracle OCI GenAI additionally needs the OCI SDK:
#   pip install oci
litellm>=1.50

# Semantic cache for the classify() step — embedded, file-based vector store
# (no server). Optional: if missing, the cache silently no-ops and the
# chatbot falls back to an LLM call for every classification.
lancedb>=0.13
pyarrow>=14.0

# Production WSGI server — gthread workers keep blocking Ansible calls
# off the request thread.
gunicorn>=22.0

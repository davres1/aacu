---
- name: MariaDB temp / undo tablespace report
  hosts: "{{ target_host }}"
  gather_facts: false
  become: true
  become_user: "{{ mysql_user | default('root') }}"

  vars:
    remote_dir: '/tmp/dba'

  tasks:
    - name: Ensure remote dir
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: '0755'
      loop:
        - "{{ remote_dir }}"
        - "{{ remote_dir }}/lib"

    - name: Copy shared library
      ansible.builtin.copy:
        src: ../files/lib/mariadb_common.sh
        dest: "{{ remote_dir }}/lib/mariadb_common.sh"
        mode: '0755'

    - name: Copy MonitorTablespaces.sh
      ansible.builtin.copy:
        src: ../files/MonitorTablespaces.sh
        dest: "{{ remote_dir }}/MonitorTablespaces.sh"
        mode: '0755'

    - name: Run MonitorTablespaces.sh
      ansible.builtin.shell: "bash {{ remote_dir }}/MonitorTablespaces.sh"
      register: result
      changed_when: false
      failed_when: false
